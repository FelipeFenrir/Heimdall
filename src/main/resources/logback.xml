<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) 2020. Fenrir Solucoes em Tecnologia. All rights reserved.
  ~  Fenrir Systems, Odin System and All the Programing Code of this softwares are private.
  -->

<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Logging into the build folder of your project -->
    <property name="LOGS" value="./logs" />

    <!-- You can override this to have a custom pattern -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" />

    <!-- Appender to log to console -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <Pattern>${CONSOLE_LOG_PATTERN}</Pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Appender to log to file -->
    <appender name="flatfile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/auth-server-logger.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily and when the file reaches 10 MegaBytes -->
            <fileNamePattern>${LOGS}/archived/auth-server-logger-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Appender to log to file in a JSON format -->
    <appender name="logstash" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/auth-server-logger.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOGS}/archived/auth-server-logger-%d{yyyy-MM-dd}.%i.json</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <pattern>
                    <pattern>
                        {
                        "timestamp": "@timestamp",
                        "severity": "%level",
                        "service": "${APP_NAME:-}",
                        "trace": "%X{traceId:-}",
                        "span": "%X{spanId:-}",
                        "pid": "${PID:-}",
                        "thread": "%thread",
                        "class": "%logger{40}",
                        "rest": "%message"
                        }
                    </pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>
    
    <springProfile name="native">
        <!-- LOG everything at INFO level -->
        <root level="INFO">
            <appender-ref ref="console" />
            <appender-ref ref="flatfile" />
            <appender-ref ref="logstash" />
        </root>
        
        <logger name="br.com.fenrir" level="DEBUG">
            <appender-ref ref="console" />
            <appender-ref ref="flatfile" />
            <appender-ref ref="logstash" />
        </logger>
        
        <logger name="com.netflix.eureka" level="OFF">
            <appender-ref ref="console" />
            <appender-ref ref="flatfile" />
            <appender-ref ref="logstash" />
        </logger>
        
        <logger name="com.netflix.discovery" level="OFF">
            <appender-ref ref="console" />
            <appender-ref ref="flatfile" />
            <appender-ref ref="logstash" />
        </logger>
    </springProfile>

    <springProfile name="development,staging">
        <!-- LOG everything at INFO level -->
        <root level="INFO">
            <appender-ref ref="flatfile" />
            <appender-ref ref="logstash" />
        </root>

        <logger name="br.com.fenrir" level="DEBUG">
            <appender-ref ref="flatfile" />
            <appender-ref ref="logstash" />
        </logger>

        <logger name="com.netflix.eureka" level="OFF">
            <appender-ref ref="flatfile" />
            <appender-ref ref="logstash" />
        </logger>

        <logger name="com.netflix.discovery" level="OFF">
            <appender-ref ref="flatfile" />
            <appender-ref ref="logstash" />
        </logger>
    </springProfile>
    
    <springProfile name="production">
        <!-- LOG everything at INFO level -->
        <root level="INFO">
            <appender-ref ref="logstash" />
        </root>
    </springProfile>
 
</configuration>